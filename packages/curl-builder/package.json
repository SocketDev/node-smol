{
  "name": "local-curl-builder",
  "version": "0.0.0",
  "private": true,
  "description": "Builds libcurl with mbedTLS for HTTPS support in stub binaries",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./lib/ensure-curl": "./lib/ensure-curl.mts"
  },
  "scripts": {
    "build": "node scripts/build.mts",
    "clean": "node scripts/clean.mts",
    "cover": "vitest run --coverage",
    "test": "vitest run"
  },
  "dependencies": {
    "@socketsecurity/lib-stable": "catalog:",
    "local-build-infra": "workspace:0.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "catalog:",
    "vitest": "catalog:"
  }
}

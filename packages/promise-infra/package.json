{
  "name": "local-promise-infra",
  "version": "0.0.0",
  "private": true,
  "description": "Source-only C++ implementation of Promise.allKeyed / Promise.allSettledKeyed (tc39/proposal-await-dictionary) for embedding into node-smol-builder. No binary release; consumers compile inline via additions/source-patched.",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./lib/paths": "./lib/paths.mts"
  },
  "scripts": {
    "check:lockstep": "node scripts/check-lockstep.mts",
    "clean": "node scripts/clean.mts",
    "test262:promise-keyed": "node test/scripts/test262-promise-keyed-runner.mts"
  },
  "dependencies": {
    "@socketsecurity/lib-stable": "catalog:",
    "local-temporal-infra": "workspace:0.0.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "local-node-smol-builder": "workspace:0.0.0",
    "vitest": "catalog:"
  }
}

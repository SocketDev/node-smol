{
  "name": "node-smol-packages-lief-builder",
  "version": "0.0.0",
  "private": true,
  "description": "Builds LIEF library for binary format manipulation",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./lib/ensure-lief": "./lib/ensure-lief.mts"
  },
  "scripts": {
    "build": "node scripts/build.mts",
    "clean": "node scripts/clean.mts",
    "cover": "vitest run --coverage",
    "test": "vitest run"
  },
  "dependencies": {
    "@socketsecurity/lib-stable": "catalog:",
    "node-smol-packages-build-infra": "workspace:0.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "catalog:",
    "vitest": "catalog:"
  }
}

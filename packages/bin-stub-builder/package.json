{
  "name": "local-bin-stub-builder",
  "version": "0.0.0",
  "private": true,
  "description": "Platform-specific stub binaries for compressed Node.js executables",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mts",
    "clean": "node scripts/clean.mts",
    "cover": "vitest run --coverage",
    "test": "vitest run"
  },
  "dependencies": {
    "@socketsecurity/lib-stable": "catalog:",
    "local-bin-infra": "workspace:0.0.0",
    "local-build-infra": "workspace:0.0.0",
    "local-curl-builder": "workspace:0.0.0",
    "local-glibc-shims-infra": "workspace:0.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "catalog:",
    "local-lief-builder": "workspace:0.0.0",
    "vitest": "catalog:"
  }
}
